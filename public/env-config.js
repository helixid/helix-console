// Placeholder for local development. In the container image this file is
// regenerated at startup from the container's environment variables by
// docker/40-env-config.sh. For `npm run dev`, leave it empty and configure
// VITE_API_BASE_URL / VITE_ADMIN_API_KEY / VITE_CONSOLE_USERNAME /
// VITE_CONSOLE_PASSWORD in console/.env instead.
